# Plan pracy — 16.09.2026

Bieżący zakres pochodzi z sekcji 18.09.2026 w pliku Michała `Do zrobienia.md` (przesłana także kopia na Telegramie). Najnowsza data wskazuje aktualne zadania. Wcześniejsze daty są historią — nie otwierać ponownie punktów tylko dlatego, że nadal widnieją w pliku.

## Uzgodnione wdrożenie — jedna sesja Spotify i podcasty

Michał zaakceptował jedną sesję Spotify z Librespot domyślnie oraz oficjalnym SDK wybieranym w ustawieniach. Zachować wspólne konto katalogu, dane bibliotek, obie kolejki, pozycje i wybory urządzeń. Nie usuwać poświadczeń. Zmiana wybranego silnika będzie jawnie opisana jako wymagająca ponownego uruchomienia AMC — bez przerywania odtwarzania podczas zapisu ustawień.

Etapy: (1) wersjonowana, idempotentna migracja i trwałe ustawienie z testem zapisu/odczytu; (2) jeden wpis sesji, rzeczywiste podłączenie wybranego wyjścia, konto i dostępna kontrolka ustawień; (3) widok podcastów Spotify, sprawdzony skrót bez kolizji, lista odcinków i usunięcie potwierdzonej blokady odtwarzania z testami; (4) pełna regresja, NVDA i osobny pomiar rzeczywistych odcinków przed meldunkiem o odtwarzaniu. Connect i wyjście TIDAL pozostają osobnymi sondami, nie są obiecywane w tym etapie.

Monitoring API, SDK i Librespot jest zaplanowany i włączony: zadanie18380ffea4c7, co poniedziałek10:00 (następny termin z harmonogramu21.09.2026, strefa+02:00). Zweryfikowano pełny prompt, nie tylko nazwę: wydania Librespot, zmiany logowania, Web API/SDK, podcasty, playlisty, jakość i Connect. Wyłącznie obserwacja źródeł; bez automatycznej aktualizacji silnika, kont, odtwarzania i zmian instalacji.

W kopii roboczej: scalona migracja018d8be (oczekuje niezależnego odbioru), API i adaptery podcastów oraz pojedynczy odczyt paska1119e77. Rodzic dodał i zmierzył dostępny wybór odtwarzacza w SettingsWindow, zapis/anulowanie/ponowne otwarcie oraz zachowanie numerów przy zapisie i ręcznym ruchu wierszy. Żywy NVDA potwierdził obie nazwy i odczyt zapisanej opcji; sonda exit0. Pełna regresja przed integracją: Windows138, Core exit0. Raport `amc_pomoc/spotify-engine-ui-review.md`. Nadal trwa podłączenie jednej sesji i klawiszy podcastów w worktree `amc-spotify-unify-wiring`; dotychczasowe zielone testy DWÓCH sesji nie potwierdzają tego przyszłego kroku. Brak próby prawdziwego odcinka; instalacja i dane Michała nietknięte.

### Integracja jednej sesji — odbiór rodzica

Kod c99efd5 nałożony do kopii roboczej rodzica BEZ commita (staged poprawki migracji nadal w odrębnym odbiorze). Rodzic uruchomił połączenie zmian na Windows: Core exit0, Windows138/138, logi `amc_pomoc/spotify-unified-parent-1`. Nie powtórzyło się pięć błędów baseline zgłaszanych przez integratora; przyczyny nie przesądzono. Wcześniejszy błąd ochrony linii Librespot nadal odbiera osobna sonda.

Odczyt rodzica wykrył brak faktycznego Alt+D w handlerach Spotify mimo gotowej metody opisu, numerowanie/ruch w Ctrl+0 nieprzystosowane do luk oraz nieprawdziwy ActiveSpotifyEngine po fallback/rebuild. Koryguje je `deleg_eca75cd6` w `amc-spotify-integration-gaps`. Dodatkowo wskazano odcinek kierowany do albumu zamiast podcastu w helperze powiązań. Nie wydawać do zamknięcia tych punktów i próby żywego NVDA z połączonym kodem.

Próba read-only na głównym komputerze nie pozyskała odpowiedzi API: odczyt po SSH zwraca1312, poprawnie wskazana interaktywna sesja konta odczytała istniejący, WYGASŁY access token. Celowo brak odświeżania tokenu i brak komend odtwarzania;0żądańAPI. Sonda, jej procesy, pliki i zadanie usunięte oraz zweryfikowane. Paragon `amc_sondy/spotify-podcasts-live-read/final-receipt.json`. To NIE dowód braku podcastów ani utraty logowania; prawdziwy odcinek nie był sprawdzany.

### Klawiatura, numery sesji i rodzic odcinka — pomiar po integracji

Nałożono 947c261: Alt+D w obu handlerach, ruch między istniejącymi numerami w Ctrl+0 i zgodność ActiveSpotifyEngine z faktycznym wyjściem po fallback/odbudowie. Rodzic zmienił przygotowanie testu UI: wybór próbnego odcinka przez prawdziwy widok zamiast uruchamiania fikcyjnego URI na rzeczywistym adapterze.

Rodzic odtworzył RED i poprawił powrót od odcinka do podcastu: kontener Podcast, URI spotify:show:, polecenie GoToPodcast także poza RSS, menu listy i odtwarzacza oraz prawa strzałka. Test wykorzystuje prawdziwe MainWindow, zaznaczony odcinek i paletę. Następny RED wykazał brak Ctrl+Alt+O w pomocy; po dodaniu do CommandPaletteSearch i TryResolveKeyboardHelpCommand test GREEN. Kontrola powiązań albumu/wykonawcy bada teraz wynik fabryki, a nie arbitralne 1200 znaków źródła.

Pełny zestaw Windows po scaleniu i poprawce kontroli źródłowej: 140/140, exit0, `amc_pomoc/spotify-podcast-parent/windows-regression-tests.log`. Ostatnie uzupełnienie pomocy dodatkowo przeszło `help-green`, exit0; pełny przebieg powtórzyć po końcowym odbiorze przywracania kolejki. Nawigację odcinek→podcast odbiera osobny recenzent; brak jego werdyktu nie oznacza zatwierdzenia.

Żywy NVDA na HERMES, prawdziwe MainWindow z prywatnym stanem: Ctrl+0 pokazuje jedną sesję Spotify pod numerem8 i zachowuje lukę2; Alt+góra przenosi WiiM3→1, Alt+dół cofa do3, Enter wraca do Spotify. Alt+D otwiera opis próbnego odcinka, fokus trafia do tekstu tylko do odczytu, strzałka w dół i read_current_line zwracają drugi wiersz, Escape wraca do tej samej pozycji. Sonda `amc_sondy/spotify-unified-gui` zamknięta, exit0, końcowe numery zgodne z początkowymi. Dane syntetyczne: to nie pomiar API ani dźwięku. Instalacja użytkownika bez zmian; nadal bramka rzeczywistego startupu kolejki i próby prawdziwego odcinka.

### Pusty start — niezależny odbiór i powtórzone testy

Przegląd deleg_0fe54fc9 zatwierdził poprawkę pustego startu: passed=true, listy błędów puste. Oczyszczanie dotyczy tylko nowej sesji przed przywróceniem cache/kolejki; istniejąca sesja przy RebuildCore pozostaje nietknięta. Rodzic powtórzył pełny zestaw w `amc-final396-spotify-tests2`: Windows141, Core exit0 i NVDA17, cały proces exit0 i źródła bez zmian podczas pomiaru. Nowy kandydat do `amc-release-396-spotify-final`; stary szkic396 (id392094152) wymaga wymiany EXE/ZIP i wskazania nowego commita przed publikacją.

### Próba prawdziwego instalatora — kandydat wymaga zastąpienia

Pierwszy nowy instalator396 z commita850b565 zainstalował się na HERMES kodem0, nowy EXE ma zgodny SHA, proces w sesji1 i odpowiedź mostka. Rzeczywisty start pustej sesji ujawnił jednak stare rekordy demonstracyjne fabryki SessionManager zamiast pustej biblioteki. Tego nie mierzyły wcześniejsze przypadki z katalogiem lub kolejką. HERMES to próba; główna instalacja użytkownika nie została zmieniona.

Rodzic dodał test dla obu silników z prawdziwym odczytem, SQLite i dwoma startami: RED „Pusta sesja Spotify pokazuje utwory demonstracyjne”, GREEN po wyczyszczeniu fabrycznych pozycji WYŁĄCZNIE przy pierwszym tworzeniu sesji, przed RestoreSpotifyCachedItems. Zachowana żywa sesja przy zmianie ustawień nie jest czyszczona. Cały dotychczasowy zestaw startupu nadal GREEN.

Katalog `amc-release-396-spotify` oznaczony DO-NOT-PUBLISH; szkic GitHub396 nie jest publicznym wydaniem i musi otrzymać nowe paczki oraz target commita. Po poprawce ponowna pełna regresja i budowa do nowego katalogu, potem powtórna instalacja na HERMES. Nie uznawać samego InstallerExit0 za pełny odbiór.

### Odbiór końcowy kodu po poprawce kolejki

Pełny przebieg rodzica na Windows zakończony kodem0: Windows141/141, Core exit0, NVDA17/17. Paragon `C:\Users\Michal\amc-final396-spotify-tests\regression-result.json`; zgodność wszystkich źródeł przez cały przebieg potwierdzona. Własna bramka pakowania zatwierdziła te wyniki i zgodność kodu. Nowe przypadki usuwania pozycji, zmiany flagi oraz pierwszej synchronizacji kolejki przechodzą razem z resztą, nie tylko osobno.

Odczytane przez rodzica logi RED podwykonawcy potwierdzają wcześniejsze przywracanie usuniętej pozycji oraz wyrzucenie kolejki przy pierwszej synchronizacji. Poprawka nałożona i zweryfikowana, blokada kodu zamknięta. Dalej pełna samowystarczalna paczka, instalator i próbna instalacja na HERMES; stare paczki396 nie służą za wynik bieżącego kodu. Dwa starsze odczyty na głównym pokazały395, radio357 w pauzie; odczyt nagrań trzeba ponowić bezpośrednio przed jakąkolwiek instalacją.

### Odbiór poprawki cyklu kolejki — 28b5032

Rodzic odczytał cały diff i nowe testy, nałożył wyłącznie commit28b5032 nad roboczym snapshotem b08afe2. EnsureQueueOrder odświeża zapis kolejki również dla Spotify, RestoreSpotifyCachedItems nie nadpisuje flag pozycji już opisanych bieżącą kolejnością, a pierwsze pobranie biblioteki zachowuje żywą kolejkę także bez wcześniejszego katalogu. Usunięty nieużywany znacznik synchronizacji Spotify; TIDAL/YouTube i audio bez zmian.

Podwykonawca odtworzył osobno trzy RED i GREEN na prawdziwych poleceniach aplikacji, pliku stanu, SQLite i dwóch startach MainWindow. Pełny zestaw Core zgłoszony z Linux NIE jest miarą Windows; rodzic ponawia oba zestawy na Windows oraz testy dodatku NVDA, z zapisem zgodności źródeł, jako `amc-final396-spotify-tests`. Nie publikować na podstawie samego meldunku podwykonawcy. Poniżej historia blokady.

### Historia blokady: pełny cykl zmian kolejki po migracji

Przegląd deleg_609ac8a1 odczytał dwie dalsze luki, potwierdzone przez rodzica w źródłach: EnsureQueueOrder aktualizuje RemoteQueues wyłącznie dla TIDAL, podczas gdy RestoreSpotifyCachedItems przy starcie nakłada stare flagi Spotify; ApplySpotifyItems odrzuca zachowaną kolejkę przy pierwszym pobraniu, jeśli katalog był pusty. Wcześniejsze zielone testy mierzyły zachowanie danych, ale nie usuwanie pozycji ani zmianę „odtwórz następne” przed kolejnym startem. final-2 NIE zamyka więc odbioru całej funkcji.

Izolowana naprawa deleg_adddb125, worktree `amc-spotify-queue-lifecycle`, baza robocza b08afe2. Kryteria RED/GREEN: prawdziwa akcja usunięcia pozycji i brak jej powrotu po Save/Load; zmiana flagi następnego odtwarzania trwała po restarcie; pierwszy ApplySpotifyItems po starcie bez katalogu nie gubi kolejki spoza biblioteki. Minimalny zakres, bez audio, kont, YouTube i zmian głównej instalacji. Konieczna ponowna pełna regresja po odebraniu poprawki. Snapshot b08afe2 nie jest commitem wydania.

ConfigurationStore z zachowaniem kluczy legacy otrzymał pozytywną ocenę; zapis/odczyt i drugi start były rzeczywiście wykonane przez rodzica. Hipoteza recenzenta o niezbadanym Save nie unieważnia istniejącego GREEN i nie wymaga zgadywania nowej poprawki.

### Pełna regresja i domknięcie nawigacji — wynik final-2

`spotify-unified-final-2`: pełny Core exit0, Windows 140/140 exit0, oba buildy bez ostrzeżeń. Wcześniejszy final-1: Windows140, Core1 wyłącznie z powodu testu oczekującego dawnego tekstu Alt+D bez Spotify. Poprawiono oczekiwanie do zmierzonego zakresu skrótu i dopisano asercję Ctrl+Alt+O; nie cofano poprawnej pomocy. Wyniki odczytane z receipt.json i logów, nie z liczby wierszy OK.

Odbiór deleg_e57ceb5e miał niespójne passed=true i niepuste listy hipotez. Odczyt rodzica rozstrzygnął dwie: FindRelatedLocalAlbum zwraca null poza ActionSession=local, a w widoku odtwarzacza ActionItem i ActionSession pochodzą z tego samego _sessions.Current. Test już potwierdza brak mylącego Album w palecie odcinka. Kodowanie PublicUri wzmocnione przez Uri.EscapeDataString z osobnym RED/GREEN, bez deklarowania nieudowodnionej podatności.

Uwaga o SearchWindow okazała się realną luką: wynik odcinka Spotify nie pokazywał „Przejdź do podcastu”. RED rzeczywistego RunSearch i menu, minimalne dodanie gałęzi Spotify, GREEN. Warunek RSS/YouTube pozostawiony. Żywy NVDA na HERMES potwierdził fokus na próbnym wyniku i pozycji „Przejdź do podcastu”; Enter zamknął dialog z RESULT=True|GoToPodcast|spotify|synthetic-search-episode, proces exit0. Sonda `amc_sondy/spotify-search-parent-gui`, bez sieci i odtwarzania.

Przegląd uzupełnień nawigacji deleg_b0983452 zakończony: passed=true, obie listy błędów puste (wynik potwierdzony w zakończonym transkrypcie). Startup deleg_609ac8a1 zablokował wydanie lukami cyklu kolejki opisanymi wyżej. Po ich naprawie przygotować NOWĄ paczkę ze wszystkich zmian, nie używać dawnego kandydata396 sprzed Spotify. Prawdziwe API i dźwięk podcastów nadal niezmierzone; nie zastępować ich wynikiem interfejsu. Główny komputer bez zmian.

### Przygotowanie paczki — bez budowy na nieodebranym kodzie

U źródła ponownie potwierdzono wydanie395 jako opublikowane prerelease; tag395 pobrany i origin/main bez nowych commitów nad HEAD rodzica. Kod natywnego hosta, dodatek NVDA, licencje, instalator i build.ps1 nie różnią się od395. Lokalne paczki źródeł Librespot i dodatku0.3.2 porównane po SHA256/rozmiarze z załącznikami395; paragon `amc_pomoc/spotify396-unchanged-assets-proof.json`.

Przygotowane prywatne skrypty: `test_final396_spotify.py` (Windows, Core, NVDA, osobny pełny katalog i zapis zgodności źródeł) oraz `build_spotify396_checked.py` (odmowa bez zielonych trzech zestawów i zgodnego kodu, potem samowystarczalny publish, Inno, ZIP, niezmieniony dodatek i źródła silnika). Negatywna próba check-only odmówiła użycia starego, niepełnego potwierdzenia final-2; żadnej paczki tym skryptem jeszcze nie zbudowano. Docelowy nowy katalog: `C:\Users\Michal\amc-release-396-spotify`. Bramka naprawy kolejki nadal obowiązuje.

### Kolejka i odczyt starszej konfiguracji — pomiar rodzica po b2c7591

Nałożono b2c7591: RestoreSpotifyCachedItems dołącza zapisane pozycje kolejki do żywej sesji także bez katalogu, przy wspólnym ID zachowuje znaczniki kolejki, a sam plik kolejki nie udaje zsynchronizowanej biblioteki. Odczyt kodu potwierdził, że raw ID trafia do sesji przez cache.ToMediaItem; zarzut koniecznego prefiksowania każdej pozycji przy starcie był nieprawdziwy. Nie odgaduje się ExternalId po tytule.

Rodzic rozszerzył test o prawdziwy odczyt ConfigurationStore, istniejące SQLite, zapis i drugi start MainWindow, rzeczywiste zakładki poza kolejką i sprawdzenie archiwum osieroconego odwołania. To wykryło DODATKOWY błąd przed konstruktorem okna: MigrateSettings odrzucał zapisane wyjście i wyciszenie spotifyLibrespot, bo usunięto ten identyfikator z listy czynnych sesji. RED: „Migracja nie przeniosla wybranego wyjscia dzwieku”. Minimalna poprawka zachowuje i waliduje klucz legacy wyłącznie przed wykonaniem wersjonowanej migracji; nie przywraca osobnej sesji.

GREEN rodzica: `startup-parent-loaded-green` i `startup-parent-sqlite-restart`, exit0. Zakładka znanego elementu wskazuje rzeczywisty wiersz bez tworzenia kolejki; osierocone odwołanie i jego pozycja są zachowane w archiwum, bez dopasowania po tytule. Prawdziwy zapis po migracji i drugi start zachowują kolejkę, wyjście, wyciszenie, numer archiwalny i jeden silnik. Logi w `amc_pomoc/spotify-podcast-parent`.

Nowe trzy diffy odbiera `deleg_609ac8a1`; pełny Core i Windows uruchomiony przez rodzica jako `spotify-unified-final-1` w oddzielnych katalogach wynikowych. Nie publikować przed odebraniem wyników i przeglądu nawigacji podcastów. Poniższe akapity opisują wcześniejsze etapy diagnozy.

### Dodatkowe sprawdzenie rzeczywistego startu — historia diagnozy

Przegląd `deleg_b4836f6a` nie zatwierdził aliasów. Dwa zarzuty były wyłącznie statyczne: pierwszy przyjmował prefiksowanie elementów przy starcie, choć RestoreSpotifyCachedItems używa cache.ToMediaItem bez ForSession; drugi zakładał możliwość odgadnięcia ExternalId z osieroconego ItemId zakładki, którego model nie przechowuje. Rodzic nie wdraża takich założeń bez pomiaru. Komentarz Ctrl+cyfra potwierdzony bezpośrednio w MainWindowShortcutRouter.ResolveDigit (Control→SessionSlot); uwaga recenzenta myliła sesje z presetami.

Jednocześnie ujawniono REALNĄ lukę: RestoreSpotifyCachedItems nie dołącza RemoteQueues[spotify] i kończy się przy pustym katalogu. Samo zachowanie kolejki w pliku nie dowodzi jej przywrócenia do żywej sesji. `deleg_5af7bca2`, worktree `amc-spotify-restore-review` baza d51bf98 (integracja + patch rodzica), ma odtworzyć i naprawić startup oraz rozstrzygnąć aliasy na prawdziwym MainWindow i zapisanym stanie, bez wymyślania brakujących identyfikatorów. Staged patch nadal niezatwierdzony do wydania.

### Zabezpieczenie zbyt długiej linii — pomiar rozstrzygnięty

`deleg_0d63ebe9` ustalił, że test podpinał HostFailed dopiero po StartAsync i gubił wcześniejszą awarię. Nie był to dowód usterki limitu w odtwarzaczu. Sonda z tym samym opóźnieniem750ms dała RED przed zmianą i GREEN po podpięciu zdarzenia przed startem; dopuszczony wyjątek startu wyłącznie LineTooLong. Rodzic odczytał diff d446a7d, przeniósł tylko test, uruchomił pełny połączony Core na Windows: exit0 (`amc_pomoc/spotify-migration-parent/transport-fix-integrated-tests.log`). Kod transportu niezmieniony. Ten bloker pomiarowy zamknięty; pozostałe odbiory migracji/klawiatury nadal obowiązują.

### Odbiór bezpieczeństwa migracji — w toku

Przegląd `deleg_cce12fb5` wykazał istotne luki mimo zielonych wcześniejszych testów. Rodzic odtworzył na Windows i poprawił osobnymi RED1→GREEN0: pełny odczyt obiektu/tablicy JSON w polu silnika bez odrzucania reszty danych, odwrócenie rzeczywistej kopii ID Librespot, niezależne archiwum presetów oraz odwołania do wybranego reprezentanta po scaleniu różnych ID tego samego utworu. Staged diff (bez commita) odbiera `deleg_b4836f6a`. Raport `amc_pomoc/spotify-migration-review-resolution.md`.

Numeracja po normalizacji/odczycie nadal należy do `deleg_cf76f4b6` (integracja jednej sesji). Pierwsza pełna regresja rodzica po poprawkach Core dała błąd ochrony zbyt długiej linii transportu Librespot; czystą bazę b55babd i przyczynę bada `deleg_0d63ebe9`. Nie nazywać tego flaky ani nie ogłaszać pełnego GREEN. Wąskie próby migracji są zielone. Dane użytkownika pozostają nietknięte; wydanie wstrzymane do odbioru całości.

## Informacja zwrotna 19.09 — Spotify, urządzenia i NVDA

Michał potwierdził odtwarzanie Spotify–Librespot oraz rzeczywiste przełączenie wyjścia przez Shift+A. Docelowo nie chce dwóch równoległych sesji Spotify; obecne zdublowanie było porównaniem. Pyta o wybór silnika, podcasty (co najmniej widoczność), Spotify Connect, utrzymanie zgodności z API oraz pamiętanie wyjścia oryginalnego TIDAL-a po odłączeniu Denona. To jeszcze nie polecenie usunięcia działającego SDK ani migracji danych.

Odczyt kodu rozstrzygnął: katalog i logowanie Web API trzeba zachować niezależnie od wybranego silnika; mamy kod pobierania podcastów, ale oba adaptery C# blokują odcinki; host Librespot nie ma włączonego Connect. Szczegóły i źródła dopisano do `SPOTIFY-LOSSLESS-I-MONITORING.md`. Jedna sesja z Librespot i ukrytym wariantem awaryjnym SDK jest rekomendacją, nie wykonaną migracją.

NVDA+End: Michał doprecyzował, że pasek działa, lecz powtarza całą treść dwukrotnie (pauza, 0:17 z 2:46, Mirafiori). Przyczyna potwierdzona: ten sam tekst był nazwą kontenera StatusStrip i jego tekstowego dziecka. Test na rzeczywistych obiektach dostępności odtworzył dokładnie podwójny tekst (RED exit1); po pozostawieniu treści wyłącznie na dziecku przeszedł dla trzech stanów (GREEN exit0). Żywy NVDA na HERMES, w prawdziwym MainWindow z prywatnym stanem i syntetycznym opisem utworu, po Insert+End wypowiedział „pauza, 0:17 z 2:46, Próba Mirafiori, Spotify — Librespot” dokładnie raz. Paragon `amc_pomoc/status-once-receipt.json`. Sonda i podgląd mowy zamknięte, proces exit0. To poprawka w źródłach, NIE zainstalowana ani opublikowana. Wcześniejsza nieważna próba bez EXTENDEDKEY nie służy za dowód.

Wcześniejszy miesięczny monitoring Lossless18380ffea4c7 zastąpiono zaakceptowaną kontrolą cotygodniową API/SDK/Librespot, opisaną i zweryfikowaną w bieżącej sekcji powyżej. Nie ma drugiego, zduplikowanego zadania.

396: test czasu życia z0d52169 został scalony, rozszerzony o1,25x i uruchomiony przez rodzica w pełnej regresji. Windows136, Core0, NVDA17/17;20s po rozgrzewce potwierdza1,000/1,250/1,499/0,752 dlaPCM16 i float32. Kandydat zbudowany w `C:\Users\Michal\amc-release-396-final`, NIE zainstalowany i NIE opublikowany. Przygotowanie wydania przerwano powyższymi pytaniami i nowym zgłoszeniem; nie wznawiać go zamiast odpowiedzi na bieżący temat. Poniżej zachowana historia etapów.

## Bieżąca kolejka — zgłoszenie 18.09.2026

1. **Aktualizacje i F11 — wydane jako 0.1.0-alpha.395 i zainstalowane na głównym komputerze.**
   - Wydanie `v0.1.0-alpha.395`, commit kodu `78b907ad3916089ccee82e6b7259ace7781fe348`: https://github.com/michalkasperczak/AMC/releases/tag/v0.1.0-alpha.395. Potwierdzono u źródła cztery załączniki, rozmiary, SHA-256, tag i status prerelease bez draft. Instalator pobrany z GitHuba na głównym ma SHA2b144f6d70213a69b5f3a7f36a1b93ad9c67ea1027d59edc0b4c924925e41817.
   - Na MICHAL instalator zakończył się kodem0, rejestr potwierdza395, uruchomiony PID32780 w sesji1 i zgodny SHA EXE. Niezależny odczyt programu potwierdza tę samą stację3, listę3 z81, pauzę i głośność50% przed i po. Nagrywanie nie było aktywne; aktualizacja nie wymusiła zakończenia nagrania. Zamykanie394 trwało dłużej niż30s, a start395 dłużej niż pierwszy limit sondy; po odczycie logów kontynuowano bez zabijania AMC i bez ponawiania instalatora.
   - Dodatek NVDA0.3.2 pozostawiony bez reinstalacji/restartu: wszystkie sześć plików zgodne z załącznikiem. W `D:\Projekty Codex\Hermes` pozostał instalator395; jeden starszy394 przeniesiono do podfolderu kosza. Kopie stanu w prywatnym LocalAppData/update-backups; paragon rodzica `amc_pomoc/install395-independent-verification.json`. Następny punkt kolejki: tempo TimeShift, bez zmian dekoderów/YouTube. Poniżej historia odbioru.
   - Odbiór `deleg_0823f708` potwierdził obie poprawki. Jedną pozostałą uwagę o mylącym zaleceniu ponownego pobrania rozstrzygnięto minimalną zmianą tekstu: test Windows najpierw exit1, po zmianie ponowne pełne Windows134/134, Core exit0, NVDA17/17. Żywy NVDA potwierdził poprawiony tekst po prawdziwym Window_Closing; sonda exit0 i StateSaved=True.
   - Ostateczny katalog to `C:\Users\Michal\amc-release-395-final3`, paragon `release-receipt.json`. Ponowna rzeczywista instalacja na HERMES: helper0, PID10836→8356, sesja1, zgodny EXE SHA54dd81f0f2fb5c8b48b0ac2874c94e1d39427bb6f9d83c1f890b3a596c1224d3. final2 i pierwszy candidate oznaczone DO-NOT-PUBLISH. Mechanizm po ed8ce35 bez dalszej przebudowy; zmieniono wyłącznie tekst i asercję. Starsze etapy poniżej.
   - Bieżący wynik po `ed8ce35`: pojedynczy odczyt pending i niezmienny plan wykonania; modalny komunikat każdej jawnej odmowy/wyjątku także po udanym zapisie. Rodzic ponowił pełną regresję na Windows: 134/134, Core kod 0, NVDA 17/17. Początkowy błąd testu Python dotyczył złej głębokości folderu hosta w sondzie, nie produktu; po poprawie układu plików pełne 17 przeszło. Żywy NVDA na prawdziwym MainWindow z prywatnym stanem potwierdził oba nowe powody błędu w realnym Window_Closing, fokus na treści, czytanie wierszami i potwierdzenie; oba procesy zakończyły się kodem 0, StateSaved=True.
   - NOWA paczka `C:\Users\Michal\amc-release-395-final2` zbudowana z aktualnych źródeł, paragon `release-receipt.json`. Próbna instalacja na HERMES zastąpiła wcześniejszego kandydata 395: helper exit0, PID9676 → PID10836, sesja1, zgodny SHA nowego EXE, świeży start i pokazane okno w logu, F11 z żywym NVDA. Nie przedstawiać tej finalnej próby jako 394→395; tamta była wcześniejsza. Końcowy krótki niezależny odbiór `deleg_0823f708` pozostaje bramką publikacji. Na głównym nadal nie instalowano; potrzebna świeża kontrola nagrań i pauzy. Poniższe wpisy opisują wcześniejsze etapy.
   - Scalone okno `2492511`, usługa `53afc5d` i podłączenie F11/menu przez `MainWindow.ApplicationUpdates.cs`. Jawna zgoda prowadzi przez zwykłe zamknięcie, ochronę nagrań i zapis; ręczna paczka nie instaluje się sama przy późniejszym zamknięciu.
   - Pełna regresja po scaleniu obu poprawek przeglądu: Windows 134/134, Core wcześniej kod 0. Żywy NVDA potwierdził rzeczywiste F11, fokus na treści, odczyt wierszami, ponowne otwarcie i powrót na listę. Dodatkowe próby 20 ponowień po anulowaniu i spóźnionego wyniku po zamknięciu również przeszły; nie wymagały zmiany produkcji.
   - Prawdziwy Inno z osobną sondą został niezależnie sprawdzony: sukces 0; uszkodzony plik 4, brak pliku 3, zablokowany cel 5. Przy błędach pending pozostaje i brak wznowienia. To instalacja sondy do pustego katalogu, NIE odbiór produkcyjnego AMC ani aktualizacji w miejscu.
   - Przegląd rozstrzygnięty: scalono `eb760e7` (ochronny zapis i trwały komunikat późnej awarii) oraz `b9eee23` (ograniczenie ścieżek pending). Rodzic poprawił dodatkowo RED→GREEN: null zamiast ścieżki i nieuzasadnioną gwarancję „żadne dane nie zostały utracone”. Test rozszerzono o prawdziwe Close/Window_Closing po zwolnieniu zasobów. Żywy NVDA potwierdził oba okna błędu: fokus na treści, czytanie wierszami, zachowanie otwartego AMC po błędzie ochronnego zapisu oraz potwierdzenie przed końcowym zamknięciem. Oba przebiegi GUI kod 0.
   - Kandydat 395 zbudowany i zainstalowany NA HERMES przez pomocnika: prawdziwe 394 → 395 w tym samym katalogu, instalator kod 0, zgodny SHA EXE, nowy proces 9676 w sesji 1, świeży App.OnStartup i F11 z żywym NVDA. Skrypt pomiarowy padł na odczycie ExitCode starego obcego procesu, ale pomocnik działał dalej; nie ponawiano instalacji, wynik zweryfikowano oddzielnie. Paragon: `C:\Users\Michal\amc-release-395-candidate\release-receipt.json`. Po ujednoliceniu wersji ponowione Windows 134/134, Core kod 0 i 17 testów dodatku NVDA na Windows (nie na Linux). Odbiór `deleg_05cd19fe` wykazał dwie zasadne luki: brak trwałego komunikatu przy odrzuconej paczce mimo udanego zapisu oraz walidacja gotowości na innym odczycie pending niż wykonanie. Pierwszy kandydat jest oznaczony `DO-NOT-PUBLISH.json` i NIE WOLNO publikować jego paczek ani używać dawnych sum jako sum finalnych. Naprawę obu sprzężonych miejsc realizuje `deleg_3c67935b` w `amc-review-final-395`, baza `0a833776fa3f78866e12e28d667085898ede8dd1`. Po odbiorze konieczna nowa paczka i ponowna próba; dopiero commit/push/release i kontrolowana instalacja na głównym. Na HERMES działa nadal pierwszy kandydat395, na głównym394. Główny komputer bez zmian.
   - Raporty: `/home/michal/projekty/amc_pomoc/update395-integration.md`, `update395-review-resolution.md`; próba Inno: `/home/michal/projekty/amc_sondy/update_inno_isolated/RAPORT-PL.md`.
2. **Rzeczywiste tempo TimeShift — w pracy po zgodzie na kontynuację.** Zmierzyć, czy zmienia się szybkość odczytu audio z bufora, a nie tylko wartość i komunikat. Sprawdzić rozróżnienie odsłuchu opóźnionego i na żywo oraz podłączenie SoundTouch. Twardy warunek Michała: NIE zmieniać parsowania, rozpoznawania formatów ani działających dekoderów radia/YouTube Live; nie cofać poprawek Opus.
   - Rodzic odtworzył fałszywe potwierdzenie w modelu sesji: odmowa silnika dawała sukces i żądane tempo, a samoczynny powrót silnika do1x nie aktualizował modelu. Opcjonalny odczyt `IPlaybackRateStateOutput` i `PlaybackRateStateTests` przeszły RED1→GREEN0 na neutralnym teście; starsze adaptery bez tego interfejsu zachowują dotychczasową obsługę. To jeszcze nie dowód rzeczywistej zmiany audio.
   - Osobny test rzeczywistego SettingsWindow na Windows odtworzył błędne Ctrl+Shift+strzałki w opisie bufora (RED1). Poprawiony tekst wskazuje skróty odczytane z kodu: Shift+przecinek, Shift+kropka, Ctrl+kropka. Pełna regresja rodzica: Windows 135/135, Core exit0 i NVDA 17/17; logi w `C:\Users\Michal\amc-timeshift396-parent`. Dotyczy zmian modelu/pomocy, przed scaleniem poprawki audio.
   - Odtworzenie i poprawka etapu audio ZA buforem: izolowany worktree `amc-timeshift-396`, branch `hermes/timeshift-396`, baza0daf0b5, delegacja `deleg_3d9acb55`. Bez kont, urządzeń wyjściowych i zmian dekoderów. Kryteria: konsumpcja materiału przy1x, wolniej i szybciej, PCM16/float32, mały zapas i dojście do live, zwykłe1x bez regresji. Nie wydawać samej poprawki komunikatów jako naprawy tempa.
   - Silnik7221c96 scalony; rodzic odtworzył i naprawił dodatkowo pięć defektów integracji realnego bufora, modelu i wyjścia (pierwsze polecenie/pauza, trwałe1x po live, End/Seek z usunięciem starych próbek, prawdziwe możliwości). Pełne Windows135/135, Core0, NVDA17/17. W1x float32 iPCM16 próbki identyczne ze starym torem; ciągły dopływ i bloki20–200ms przechodzą. Zapas/prog UI ujednolicony1s bez zmiany buforaEnd0.25s. Odbiór niezależny deleg_d963d1d2 ma bazę sprzed tych uzupełnień. Raport `amc_pomoc/timeshift396-integration-checks.md`; do zrobienia rzeczywista stacja/tor wyjściowy i żywy NVDA, potem końcowy odbiór oraz wydanie.396 nadal niewydane.
   - Po odbiorze sprzed integracji dodano RED1→GREEN0 jednorazowy komunikat powrotu do normalnego tempa. Rzeczywisty RadioMediaOutput na publicznych AAC/MP3 Radia Nowy Świat przechodzi; stary MediaFoundationReader daje PCM16, bez zmian dekodera. Osobno rzeczywisty WasapiOut (celowo wyciszony, bez ręcznych odczytów) konsumuje materiał zgodnie z1/1,25/1,5/0,75/1x. Żywy NVDA z prawdziwym MainWindow i zapisem z podglądu mowy potwierdził pierwsze ustawienie, zmianę w pauzie, reset, End, odmowę na live i pojedynczy automatyczny komunikat; nie było fałszywego „niedostępna w tej sesji”.
   - Najnowsza pełna regresja: Windows135 (licznik końcowy, nie liczba liniiOK), pełny Core0, NVDA17/17. Raport `amc_pomoc/timeshift396-live-nvda-review.md`, pomiary `C:\Users\Michal\amc-timeshift396-live`, `amc-timeshift396-gui` i `amc-timeshift396-final-regression`. Nadal NIEWYDANE; główny komputer pozostał bez zmian. Do odebrania osobna delegowana seria czasu życia/20-sekundowych pomiarów w worktree `amc-timeshift396-lifetime`, potem finalne przygotowanie396.
3. **TIDAL: rozpoznanie bieżącego utworu i kolejka.** Sprawdzić możliwość wskazania odtwarzanego utworu na liście AMC i podążania za nim. Zmierzyć „Odtwarzaj jako następny” i „Dodaj do kolejki”: naprawić realnie wspierany przebieg, a gdy dany tryb nie pozwala wykonać operacji, usunąć pozorną funkcję. Nie wnioskować o możliwościach z samych napisów ani powtarzać starej ogólnej tezy o braku sterowania bez konfrontacji z pomiarami CDP/SMTC.
4. **Apple Music.** Biblioteka, nawigacja i odtwarzanie odpowiednie do udowodnionych możliwości. Uwzględnić wcześniejsze ustalenia o MusicKit i oryginalnej aplikacji; nowy wpis nie jest zgodą na zakup płatnych usług.
5. **Google Cast/Chromecast.** Zbadać i wdrożyć przesyłanie w granicach możliwości źródeł i urządzeń. Nie obiecywać przesyłania chronionego audio bez dowodu.

### Informacja zwrotna po 394 — Telegram 5681

Michał potwierdził działanie YouTube i nowej sesji Spotify przy odtwarzaniu albumów. Kolejki i następnego utworu jeszcze nie sprawdził. YouTube zostawić bez zmian.

- Ulubione: utwory; odczyt tytuł, potem wykonawca, jak wewnątrz albumu. Sprawdzić rozdzielenie zapisanych albumów/wykonawców do Biblioteki, bez kasowania danych konta.
- Alt+3: nowe pozycje mają trafiać na początek. Zachować względną własną kolejność już obecnych pozycji, nie zamieniać tego trybu w sortowanie alfabetyczne.
- Prawa strzałka → wykonawca, HTTP 400: poprawka `b3931c7` odczytana i scalona do kopii roboczej. Osobny `ArtistAlbumsPageSize=10` tylko dla albumów wykonawcy; globalny PageSize i działające odtwarzanie bez zmian. Dokumentacja Spotify potwierdza Maximum 10. Rodzic wykonał własne RED (sam scalony test, stary limit: HTTP 400 i kod 1), potem GREEN (poprawiony limit: wszystkie 133 testy Windows, kod 0). Test mierzy dwie strony po adresie next, pełne sześć pozycji próbnych i relacje wykonawcy. To pomiar na atrapach HTTP, nie próba konta Michała. Dzienniki: `C:\Users\Michal\amc-update-routing-parent\artist-red-tests.log` i `artist-green-tests.log`. Wydane i zainstalowane w395.

Starsze otwarte uwagi dotyczące Spotify (pełna nawigacja na starym cache, playlisty/sortowanie) zachowują swój status, ale nie zastępują powyższych priorytetów. Poniższa kolejność z 16.09 jest zapisem historycznym, nie aktualnym poleceniem pracy.

## 1. AMC — Hermanice urywa, TVP Info i Rzeszow "strumien prywatny"

Zgloszenie Michala 16.09: "Znowu urywa Hermanice, dalej strumien prywatny.
Takze cos jest ewidentnie nie tak w tych Twoich pomiarach."

Michal ma racje co do pomiarow. 15.09 zmierzylem zaciecia TYLKO na TVP Info,
90 s, jeden kanal, i na tej podstawie oglosilem przyczyne. Hermanic NIE
zmierzylem ani raz — poprawke (SelectAudio fallback na muxed) zrobilem z
odczytu kodu i zapisalem jako naprawiona bez przebiegu. Dlatego wraca.

Pomiar w toku: testy/pomiar_hermanice_i_zaciecia.ps1 na michal-glowny,
skladnikami AMC, trzy kanaly naraz (txtC7cxR6m8, NRIOcMdE4DM, 3jKb-uThfrg).
Rozdziela DWA rozne objawy, ktore wczesniej mieszalem:
- "prywatny strumien" = SelectAudio nic nie wybiera (blad rozpoznania zrodla)
- "urywa" = strumien sie otwiera, ale dzwiek ustaje w trakcie (blad potoku)

Zasada na przyszlosc: kazdy kanal ze zgloszenia mierzyc osobno. Jeden zmierzony
kanal nie jest dowodem dla pozostalych.

## 2. AMC — TIDAL: domknac sprawe sterowania

STAN ZMIERZONY 11.09 (nie zgadywany):
- Sterowanie oryginalna aplikacja DZIALA przez SMTC
  (GlobalSystemMediaTransportControlsSessionManager). Zmierzone: pauza, gra,
  nastepny, poprzedni, stop. Pelne utwory (105 s grania, dlugosc 4:09).
  Dziala przy ukrytym oknie. Nie lamie regulaminu — dzwiek odtwarza TIDAL.
- Do pauzy uzywac TryTogglePlayPauseAsync, NIE TryPauseAsync
  (IsPauseEnabled=False, IsPlayPauseToggleEnabled=True).
- Filtrowac wlasny proces: AMC samo publikuje sesje SMTC (petla).
  Filtr: zawiera "tidal", nie zawiera "webview".
- TIDAL nie zglasza POZYCJI (zawsze zero) — liczyc samemu Stopwatchem.
- Pelne odtwarzanie w NASZYM odtwarzaczu jest niemozliwe i to jest zamkniete:
  modul Player w SDK to jedyna dozwolona droga i daje wylacznie probki.
  10 zgloszen w GitHub Discussions org:tidal-music bez odpowiedzi TIDALa.
  High Tide gra cale utwory, ale podszywa sie pod klienta mobilnego — NIE
  kopiowac.

BRAKUJACE OGNIWO (jedyna otwarta rzecz): przekazanie WYBRANEGO utworu do
oryginalnej aplikacji. tidal://track/<id> otwiera aplikacje, ale gra co innego
— zmierzone na dwoch utworach (328626303, 595283). Warianty nieskuteczne:
tidal://tracks/, tidal://play/track/, tidal.com/track/, listen.tidal.com/track/.

NIEWYCZERPANE, do zmierzenia dzis:
- wpisanie utworu w wyszukiwanie samej aplikacji TIDAL (klawiatura/UIA)
- kolejka aplikacji
- czy da sie ustawic kontekst odtwarzania przez API przed TryPlayAsync

Kryterium sukcesu (nie "cos gra", ale "gra TO"): tytul z SMTC zgodny z
oczekiwanym, na DWOCH roznych utworach. Jesli nie wyjdzie — zamykamy TIDALa
przy tym, co dziala: nasze przegladanie + sterowanie transportem, wybor utworu
recznie w TIDALu. I mowimy to Michalowi wprost.

## 3. Spotify

### Bieżący odbiór alfy 394

Druga sesja jest połączona z głównym oknem i osobnym logowaniem; pakowanie hosta scalone z `4ca60fdbddb438b155c684e533bb311a1a929363`. Dołączono pełny inwentarz Cargo.lock, oryginalne materiały licencyjne i dodatkowy ZIP źródeł składników. Wszystkie 365 archiwów rejestru porównano z sumami Cargo.lock.

Końcowy `build.ps1 -Publish` wykonany w interaktywnej sesji Windows na MICHAL przeszedł wraz z Core i 129 testami Windows; 17 testów dodatku NVDA także przeszło. Publikacja jest samowystarczalna. Instalator zainstalowany próbnie na HERMES, exit 0; SHA zainstalowanego programu zgodny z ZIP-em, SHA hosta zgodny z pinem. Uruchomiona rzeczywista aplikacja, nie harness: żywy NVDA potwierdził nową sesję, Ctrl+F5 i Shift+A. Oba silniki Spotify i moduł TIDAL są w paczce. Porównanie całego kodu z archiwum użytym do kompilacji nie wykazało różnic.

Pliki końcowe: `amc_release_394/AMC-Setup-0.1.0-alpha.394.exe` (65 199 072 B, SHA-256 `d9b0528723f3c32f6cd7897421586fe1a3a2771070327cdf13399adf8974ce0a`), `AMC-0.1.0-alpha.394.zip` (87 739 504 B, `a35a7d6950c98512e14bc308f068a24e529e77d8a9cbd3393abf83acd14584e6`), dodatek `AMC-NVDA-0.3.2.nvda-addon` oraz `AMC-Librespot-Sources-0.1.0-alpha.394.zip`. Zdalne nowe paczki są w `C:\amc_spotify_fixes\final-release-394`, NIE w starym `C:\amc_release_394`. Wydanie `v0.1.0-alpha.394` opublikowane z commita `5231382d7dfaf783b37c167d23c496d017040913`; GitHub API potwierdziło 4 załączniki, wszystkie rozmiary i sumy. Instalator pobrany ponownie z GitHuba ma tę samą sumę. Na MICHAL wykonano instalację (exit 0) i uruchomiono nową aplikację w sesji 1; rejestr odinstalowania oraz SHA pliku potwierdzają 394. Sześć plików dodatku NVDA 0.3.2 jest zgodnych z paczką, czytnik został ponownie uruchomiony w sesji graficznej. Pierwszy odczyt IPC po starcie nie przeszedł, późniejszy prawidłowo odpowiedział; nie przedstawiać pierwszej odmowy jako awarii instalacji. Przed samym restartem użytkownik przeszedł z podcastu do Spotify; odtwarzacz był w pauzie. Po restarcie Spotify nie miał otwartego nagrania — NIE potwierdzono przywrócenia poprzedniego pauzowanego elementu. Nie ogłaszać zachowania podcastu na podstawie wcześniejszego odczytu. Kopie stanu i dodatku są w prywatnym `LocalAppData\AccessibleMediaController\update-backups`. Najnowszy instalator jest w `D:\Projekty Codex\Hermes\AMC-Setup-0.1.0-alpha.394.exe`; wcześniejsze instalatory przeniesiono do podfolderu kosza. Niezależny odczyt instalacji zapisany poza repo w `amc_pomoc/install394-independent-verification.json`.

Poniższe zapisy opisują kolejne etapy i wcześniejsze blokady; nie zastępują powyższego bieżącego stanu.

### Librespot — zgoda na integrację 18.09, Telegram 5622

Michał: „To integrujemy, chyba warto by”. Dodać jako DRUGI silnik wbudowanego odtwarzania Spotify, zachowując obecny Web Playback SDK i możliwość powrotu. Nie mylić Librespot z Lossless: upstream nie obsługuje Spotify Lossless.

Doprecyzowanie Telegram 5626: dalej testować, przedstawić końcowy sensowny wariant całości, oczywiste poprawki wdrażać samodzielnie; o niepewne decyzje pytać.

**Zmiana decyzji, Telegram 5633:** zamiast przełącznika w jednej sesji udostępnić w JEDNYM programie i JEDNYM instalatorze DWIE równoległe sesje: dotychczasową „Spotify” z Web Playback SDK oraz „Spotify — Librespot”. Nową dopisać na końcu bez zmiany dotychczasowych slotów i skrótów. Oddzielić kolejki, bieżący utwór, pamięć ustawień oraz wyjście dźwięku; dane konta Spotify mogą być wspólne tylko wtedy, gdy zezwala na to rzeczywisty mechanizm logowania. Nie dublować bez potrzeby kodu API i interfejsu, ale też nie wymieniać silnika pod starą sesją. Po testach Michał zdecyduje, czy usunąć jedną sesję albo przenieść część rozwiązań.

Poprzedni wariant „jedna sesja z wyborem silnika” jest nieaktualny. Zewnętrzne Spotify/Connect pozostaje osobnym kierunkiem Lossless, nie właściwością Librespot. Logowanie albo test przerywający muzykę wymaga udziału/uzgodnienia z Michałem.

Etapy:
1. Sonda na Windows uruchomiona: upstream v0.8.0 zbudowany w GitHub Actions; na Hermesie version/help/backends/devices exit0 po dodaniu lokalnej zależności CRT. Odczyt na głównym pokazuje Realtek, Denon PMA-1700NE i Yeti. Bez odtwarzania, logowania i zmian miksera. Docelowy wrapper ma być samowystarczalny (statyczny CRT), bo sam exe z CI wymagał VCRUNTIME140.dll. Odtwarzania na koncie jeszcze nie potwierdzono. Dowody: `amc_sondy/librespot/RAPORT.md`, `measurements-windows-crt.json`, run GitHub `35373143087`.

W Core powstała rejestracja osobnej sesji bez przestawiania poprzednich slotów, osobne pozycje/opcje/urządzenie oraz niezależne kopie elementów i flag kolejki. Testy tych zachowań przeszły czerwony→zielony; pełna regresja Windows po tych zmianach przeszła (build/Core/Windows exit 0). Wyszukiwanie zachowuje docelową sesję i jej identyfikatory; cykliczny zapis i odtwarzanie pozycji obejmują obie sesje osobno. Nowe testy najpierw wykazały brak tej obsługi, potem przeszły. To fundament, jeszcze bez wystawienia drugiej sesji w głównym oknie.
2. Adapter odtwarzania za istniejącym IMediaOutput: utwór, pauza, czas, seek, kolejka, zdarzenia końca i błędów, wybór wyjścia. Testy różnicujące i zachowanie dotychczasowego silnika.

Proces pomocniczy Rust zbudowany na gałęzi `hermes/librespot-probe`, commit `8c327d60bf353b3b7c5dd757e3289679d57900da`. Odczyt GitHub API potwierdził oba zadania runu `35377465516` jako `success`, w tym uruchomienie protokołu JSON i sprawdzenie braku importów VC++ Runtime. Artefakt hosta: `amc-spotify-librespot-host-windows`, id `10560514094`. Rodzic następnie uruchomił dokładnie ten plik na HERMES i MICHAL: SHA-256 `0a7f88c8185187a1889110f23084a4097505f12867cf64e0bf8e94bd16553bd3`, exit 0, siedem komend i osiem prawidłowych zdarzeń JSON na każdej maszynie; rzeczywiste urządzenia audio, odrzucenie obcej sesji, błędnego JSON, pauzy przed logowaniem i nieobsługiwanej prędkości. Bez dodatkowej DLL CRT, konta, muzyki ani zmian miksera. Dowody: `amc_sondy/librespot/host-35377465516/probe-{hermes,main}-noaccount.json`. Przed integracją trwa weryfikacja dwóch ryzyk w trackerze zdarzeń: szybkie kolejne Play przed przypisaniem identyfikatora silnika oraz seek podczas pauzy. Moduł C# scalony lokalnie jako `0143046` po checkpointcie `d81b160` (Core 137, Windows 100 i Python 17 przeszły na Windows). Osobna sonda na HERMES uruchomiła rzeczywisty klient C# z rzeczywistym hostem Rust: handshake, urządzenia, ping, odmowa pauzy przed logowaniem, następny ping i poprawne zamknięcie przeszły. Wynik: `amc_sondy/librespot/client-probe/`, pomiar `C:\Users\Michal\amc-librespot-probe\client-probe\result-hermes.json`. Wykryta konkretna niezgodność: `SetOutputDeviceAsync` C# wysyła drugie `initialize` z pustym tokenem i polami `keep*`, których Rust nie realizuje. To NIE jest działający wybór wyjścia; wymaga uzgodnionej obsługi cyklu życia procesu i poprawnego wznowienia. Adapter wymaga też podłączenia startu/logowania na żądanie, zanim trafi do okna. Osobno trwa naprawa bariery w testach C# i ocena przeżywających mutacji. Nie ogłaszać gotowości drugiej sesji ani instalatora na podstawie samych testów transportu.
Dodatkowe poprawki rodzica po scaleniu C#:

**Próba na koncie i zmiana założenia logowania:** po zgodzie Telegram5651 sonda na MICHAL odczytała Premium (`/me` 200), odświeżyła i zweryfikowała zapis tokenu SDK, a host przyjął initialize. Odtwarzanie jednak odpadło w Login5 z `INVALID_CREDENTIALS`; drugi utwór „Plastelina” (`7cxb4tW2mdCRhzg1K5BQwo`) miał metadata 200 i `is_playable=true`. To nie problem konta Premium ani dostępności utworu. AMC PID23672 nie zmienił się, nagrania nie zatrzymywano. Dowód: `amc_sondy/librespot/account-probe/latest-account-result.json`.

Upstream #1737 i go-librespot #364 potwierdzają zamknięcie prywatnego odtwarzania dla tokenów własnego klienta Web API. **Nie używać tokenu obecnej sesji SDK do Librespot.** Nowe logowanie: udokumentowany przez upstream standardowy device flow (PR1745, merge `1599145bf2c98660d35b17817b3386767a7e4b42`), strona spotify.com/pair. W sondzie uruchomione osobne parowanie; token ma trafić wyłącznie do odrębnego celu Menedżera poświadczeń `AccessibleMediaController/SpotifyLibrespot`, nigdy pod cel starego Spotify. `pair-and-probe.ps1` na MICHAL czeka na zatwierdzenie użytkownika, potem sam wykona pomiar odtwarzania. Wyniki: `C:\amc_librespot_account_probe\pair-status.json`, `result.json`, `exit.txt`. Parowanie ZAKOŃCZONE powodzeniem: osobny wpis poświadczeń zapisany i odczytany. Sonda odtworzyła „Plastelinę”, pokazała rosnącą pozycję oraz sygnał WASAPI należący do procesu hosta na Realtek. Pauza, przewijanie na pauzie i wznowienie przeszły; proces hosta zakończył się, AMC PID23672 nie zmienił się. Dowody lokalne `paired-status.json` i `paired-playback-result.json` w `amc_sondy/librespot/account-probe`. NIE ponawiać parowania, gdy można użyć tego zapisu. Osobne poświadczenia trzeba podłączyć do docelowej sesji; dotychczasowy szkic fabryki w `MainWindow.SpotifyLibrespot.cs` jeszcze błędnie pobiera token SDK i wymaga zmiany przed wystawieniem sesji.

**Review 5ac809b:** sześć poprawek przygotował osobny worktree, commit `17d9be5f1472124af29817d84473551946337861`. Rodzic odczytał diff i przeniósł zmiany, zachowując własne testy. Na Windows ponownie uruchomione przez rodzica: `--librespot-review-regressions`, `--librespot-lifecycle`, `--librespot-preparation-races`, `--librespot-output-smoke` — wszystkie exit 0. Są to testy atrap protokołu, nie odsłuch. Odbiór całej integracji nadal przed wydaniem.

**Okno parowania:** `SpotifyLibrespotAccountWindow.xaml(.cs)` ma nazwane pola tylko do odczytu z kursorem, jawne otwarcie strony, anulowanie oczekiwania i oddzielne odłączenie. Test najpierw wykazał brak okna; po implementacji przepływ na atrapach przeszedł. Dwie mutacje (usunięcie otwarcia adresu, usunięcie Cancel) wykryte przez test; po przywróceniu exit 0. Żywy NVDA na Hermesie potwierdził instrukcję, próbny kod TEST42, adres oraz przyciski otwarcia strony i kopiowania; Escape zamknął okno. Była to jawna próba bez konta i sieci. Obsługa poświadczeń z `hermes/librespot-auth`, commit `943e3ca730d4bf83848ca6843f60475cd5a70c6c`, została przeniesiona do głównej kopii. Rodzic uruchomił na Windows `--librespot-auth`: HTTP na atrapach i prawdziwy roundtrip losowego testowego klucza WinCred przeszły. Podczas odbioru wykryto ryzyko odtworzenia skasowanego logowania przez spóźnione odświeżenie. Poprawka `68dfe839778a3755baf293104543e9b27fe0ddae` została odczytana i scalona: numer generacji blokuje późny zapis, odłączenie nie czeka na HTTP, błędy serwera nie trafiają surowo do komunikatów, adresy z UserInfo i obcym portem są odrzucane. Rodzic potwierdził w kodzie, że Dispose NIE usuwa zapamiętanego konta. Końcowa pełna regresja po scaleniu pozostaje do ponowienia przed wydaniem. Okno używa wspólnej walidacji adresu HTTPS (również www.spotify.com, błąd potwierdzony czerwony→zielony) i daje przejście do dotychczasowego konta katalogu/biblioteki. Podłączenie MainWindow z `hermes/librespot-window` (commit `c1083b4298544eb72ceba72e1dfc8f4128430f5f`) zostało scalone przez rodzica. Fabryka używa już `GetSpotifyLibrespotAccessTokenAsync`, bez tokenu SDK; zamknięcie zwalnia osobno usługę natywną. Normalny build, z ostrzeżeniami traktowanymi jako błędy, oraz osiem izolowanych trybów testowych przeszły. Pełna regresja: Core na MICHAL przez SSH 138 komunikatów OK, exit 0; pełne Windows na HERMES z kopią źródeł 129 testów OK, exit 0. Windows przez SSH na MICHAL padł wyłącznie w próbie WinCred (1312, brak sesji logowania); tę samą próbę i cały zestaw potwierdzono w sesji Windows Hermesa, bez pomijania testu.

**Główne okno — pomiar NVDA:** harness `/home/michal/projekty/amc_sondy/librespot/ui-harness`, publikacja `C:\Users\Michal\amc-librespot-ui`, osobny stan `Temp\amc-librespot-ui-fixture`. Prawdziwe MainWindow, próbne elementy i fikcyjne odtwarzanie, odrębny losowy testowy target poświadczeń (bez czytania konta użytkownika). Ctrl+F5 otworzył właściwe konto Librespot z fokusem na instrukcji. Przejście do konta katalogu i powrót przez Escape potwierdzone. Shift+A odczytał rzeczywiste urządzenia przez przypięty host, NVDA odczytał Realtek; wybór zapisany w `settings.spotifyLibrespotDeviceName`, po zamknięciu i nowym uruchomieniu odtworzony w oknie. Powrót fokusu na ten sam wiersz listy potwierdzony. Niezależny od próby interfejsu **pomiar rzeczywistego przełączenia na MICHAL również przeszedł**: sonda użyła skompilowanego `SpotifyLibrespotMediaOutput` i `SpotifyLibrespotAuthenticationService`, nie własnej imitacji adaptera. „Plastelina” grała najpierw na domyślnym Realtek (sygnał WASAPI przypisany PID36448), po `TrySetOutputDeviceAsync` nowy PID37844 wznowił pozycję i dawał sygnał wyłącznie na Denon PMA-1700NE. Stary proces zniknął przed pomiarem; pauza, seek na pauzie i resume przeszły. Sonda zakończyła hosta, AMC PID23672 nie zmienił się, miksera/rejestru nie ruszano. Dowód odczytany z dokładnego zdalnego celu: `amc_sondy/librespot/output-probe/result-main.json`. Pakowanie hosta do pojedynczego instalatora jest w osobnym worktree `amc-librespot-package`. Nie wydawać przed połączeniem ostatnich poprawek auth, pakowania i próbą gotowej paczki. Końcowy build przeszedł z normalnymi ustawieniami (tymczasowe wyłączenie błędów ostrzeżeń nie jest już potrzebne).

- Rust: końcowy commit `791c8dbf2b3279f1bb8fee9220bf95d714168c96`, run `35381167099` ma oba zadania `success` (odczyt API rodzica). Rodzic pobrał logi RED `35380778719` i GREEN: pięć regresji najpierw padło, potem testy jednostkowe 30/30 i procesowe 13/13 przeszły. Poprawiono kolejność wiązania szybko zmienianych utworów oraz zachowanie pauzy przy przewijaniu. Cargo.lock jest w źródłach, build i test używają `--locked`. Kod Rust i workflow przeniesione do głównej kopii roboczej; artefakt id `10562410937`, SHA-256 exe `f5b614f01d8943b5872d9a2bb86658761198b763bb25c1647aa79268a2473c8e`. Rodzic ponownie uruchomił z nim rzeczywisty klient C# na Hermesie, bez konta: PASS. Dowód: `C:\Users\Michal\amc-librespot-probe\client-probe\result-hermes-35381167099.json`.

- Obsługa konta, opcji, kolekcji i nawigacji rozpoznaje oba identyfikatory sesji. Cache albumów i ich modele są rozdzielone; ponowne otwarcie nie zeruje kolejki. Istniejące okno wyboru audio otrzymało osobny konstruktor przyjmujący listę urządzeń silnika, bez podmieniania ich nazw na identyfikatory innego katalogu. Testy Windows każdego z tych rozszerzeń najpierw pokazały brak, potem przeszły; okno zbudowano na danych próbnych bez pokazywania. Żywy NVDA i podłączenie do nowej sesji jeszcze przed nami.

- Adapter miał rzeczywisty wyścig przed komendą Play: pauza/stop podczas oczekiwania na potwierdzenie głośności dopuszczały późniejsze uruchomienie. Test Windows najpierw odtworzył błąd; wersja zamiaru i atomowa rejestracja Play go usuwają. Trzy scenariusze (pauza, stop, nowszy utwór przed późnym A) przeszły w dziesięciu przebiegach; starszy test adaptera też przeszedł. `SpotifyLibrespotPreparationRaceTests.cs`, flaga `--librespot-preparation-races`.

- Potwierdzone zmiany Ulubionych/Biblioteki są nakładane na obie sesje wspólnego konta, bez współdzielenia obiektów i flag kolejki. Test Windows czerwony→zielony sprawdza dodanie, usunięcie, deduplikację oraz brak Stop na obu wyjściach. `SpotifyNativeCollectionTests.cs`, flaga `--spotify-native-collection`.

- Osobny etap `hermes/librespot-lifecycle` dopina fabrykę klienta, odroczone uruchomienie/logowanie, ponowne tworzenie procesu przy zmianie urządzenia i bezpiecznik przygotowania. Rodzic nie uznaje tej funkcji za gotową przed odbiorem kodu i próbą całości.

3. Dostępna DRUGA sesja „Spotify — Librespot”, niezależne ustawienia i Shift+A; zapis/odczyt, brak zmiany starej sesji i numerów skrótów, żywy NVDA, próba na koncie dopiero w uzgodnionym momencie.
4. Wydanie, instalacja, uruchomienie i weryfikacja; nie wydawać fundamentu bez funkcji dla użytkownika.

Lossless pozostaje oddzielnym wariantem z oryginalnym Spotify/zgodnym Connect. Sterowanie oficjalnym Web API ma lepsze możliwości niż obecny pilot TIDAL (konkretny URI, rzeczywista pozycja, seek), ale nie zostało jeszcze zmierzone na koncie. Pełne ustalenia i źródła: `SPOTIFY-LOSSLESS-I-MONITORING.md`.

### Doprecyzowanie Michala 18.09 — obsluga jak w TIDAL

Odtwarzacz Spotify juz gra — nie przebudowywac go. Ujednolicic pozostala
obsluge z TIDAL, honorujac granice oficjalnego API.

Zakres zgloszenia:
- Ctrl+Shift+E/R/T: odczyt czasu i przechwytywanie klawiszy;
- strzalka w prawo: wykonawca -> albumy, album -> wykonawca;
- Alt+Shift+Enter: rzeczywiste opcje pozycji;
- Alt+Enter: kursor/czytanie NVDA we wlasciwosciach;
- jawna informacja o jakosci Spotify (nie nazywac strumienia Lossless bez dowodu);
- porownanie pozostalych akcji TIDAL/Spotify (wyszukiwanie, kolekcja, kolejki,
  playlisty, sortowanie) i podpiecie tego, na co API pozwala;
- Shift+A pozostaje otwarte, bez kolejnych eksperymentow z rejestrem usera.

Stan roboczy: przygotowywana alfa 394, JESZCZE NIEWYDANA. Zintegrowano opcje pozycji, relacje, zdalne wyszukiwanie i zapis biblioteki przez API. Weryfikacja Windows obu zestawów przeszła; dodatek ma 17 testów Python na Windows na zielono. Review wychwycił utratę częściowego potwierdzenia przy timeoutach oraz duplikat po zapisie z wyszukiwania — poprawione z testami czerwony/zielony. Nie użyto prawdziwego konta do prób zapisu.

Na Hermes żywy NVDA potwierdził odczyt czasu przy 9/9 powtórzeniach na symulowanym odtwarzaniu, opcje albumu wraz z zapisem, menu relacji oraz właściwości z natywnym kursorem. Dodatkowo 4/4 przełączenia F6 tekst/HTML, odczyt wiersza NVDA+góra i powrót na listę są potwierdzone. Ważne: przy przywracaniu WindowsFormsHost trzeba odczekać na układ okna przed przeniesieniem fokusu. `getScript` dodatku sprawdza rzeczywisty tytuł okna Win32, bo obiekt foreground NVDA bywa nieaktualny w wątku klawiatury.

Źródła i testy Windows: `C:\amc_spotify_fixes`, nie cudzy brudny projekt na D:. Instalacja Michała niezmieniona. Robocze paczki `C:\amc_release_394` powstały PRZED ostatnią poprawką przywracania fokusu i NIE zawierają drugiej sesji Librespot — nie publikować ich jako końcowego wyniku, wymagają ponownego builda. Pełna nawigacja na starym cache, playlisty/sortowanie, nowa sesja Librespot oraz próba na prawdziwym koncie pozostają otwarte.

Jakosc u zrodla: https://support.spotify.com/us/article/audio-quality/
podaje dla Web Player Premium AAC256, a Lossless FLAC w aplikacjach
komputerowych/mobilnych. Publiczny Web Playback SDK nie raportuje pola
jakosci/bitrate w udokumentowanym stanie. Nie zmierzono chronionego strumienia
u Michala i nie wolno podawac parametrow z dokumentacji jako pomiaru utworu.

Michal 14.09: "najprosciej", ma byc pierwsze z serwisow. Web Playback SDK w
WebView2 gra CALE utwory dla konta Premium, bez zgody Spotify na start.
Zakres "streaming".

Pulapki ustalone u zrodla:
- konto musi byc Premium (nie mobile-only)
- uzycie komercyjne wymaga pisemnej zgody Spotify
- Developer Policy ZABRANIA crossfade i nakladania dzwieku — funkcje AMC
  "gladkie przejscia" i "cisza miedzy utworami" dla Spotify sa zabronione,
  trzeba je wylaczyc, nie tylko pominac

## 4. Apple Music

Mozliwe, ale nie natywnie na Windows. MusicKit natywny istnieje tylko dla
systemow Apple. Dla Windows jedyna droga to MusicKit JS v3 w WebView2.
Wymaga tokenu dewelopera podpisanego kluczem z platnego Apple Developer
Program (99 USD/rok) — to decyzja Michala o pieniadzach, zapytac PRZED praca.
Drugie ryzyko: DRM Widevine w WebView2 (dziala od 1.0.818.41, wymaga HTTPS,
ale zgloszenie #4828 opisuje braki w niektorych konfiguracjach) — zmierzyc,
nie zakladac.
